export { encodeQr as encode } from './encoder';
export {
	toCanvas as renderCanvas,
	toCanvasAsync as renderCanvasAsync,
	toDataUrl as renderDataUrl,
	toImageData as renderImageData,
	toImageDataAsync as renderImageDataAsync,
	toPngBlob as renderPngBlob,
	toSvg as renderSvg,
} from './render';
